<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CameraManager"
        tools:targetApi="31">
        <activity
            android:name=".CameraRepository"
            android:exported="false" />
        <activity
            android:name=".CameraViewModel"
            android:exported="false" />
        <activity
            android:name=".CameraDatabase"
            android:exported="false" />
        <activity
            android:name=".CameraDao"
            android:exported="false" />
        <activity
            android:name=".ViewCameraActivity"
            android:exported="false" />
        <activity
            android:name=".CameraConfigActivity"
            android:exported="false" />


        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.CameraManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CameraManager" /> <!-- Other Activities -->
        <activity android:name=".AddCameraActivity" />
        <activity android:name=".CameraFeedActivity" />
    </application>

</manifest>